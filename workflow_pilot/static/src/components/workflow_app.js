/** @odoo-module **/

import { Component } from "@odoo/owl";

// WIP: this component will eventually host the full workflow builder UI.
// For the current playground, see `workflow_pilot/static/dev.html` + `static/src/dev_boot.js`.

export class WorkflowApp extends Component {
    static template = "workflow_pilot.workflow_app";
}