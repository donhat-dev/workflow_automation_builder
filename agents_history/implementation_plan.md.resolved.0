# Goal: Create Rete.js Plugin & Workflow Builder Guide

The goal is to create a detailed document `workflow_automation_builder/rete_workflow_builder_guide.md` explaining how to build a custom Rete.js plugin and integrate Rete.js to create a UI Workflow Builder, strictly following the provided [llms-full.txt](file:///c:/Users/ODOO/Documents/workflow_automation_builder/llms-full.txt) documentation.

## User Review Required
> [!IMPORTANT]
> This guide focuses on **Lit** as the integration framework, as implied by your open files. If you prefer another framework (React, Vue, Angular), please let me know.

## Proposed Document Structure

The document will cover:

1.  **Core Concepts**: Brief overview of `NodeEditor`, `AreaPlugin`, `DataflowEngine`/`ControlFlowEngine`.
2.  **Building a Simple Plugin**:
    *   Explanation of the `Scope` and `Pipe` system.
    *   Example: A simple logger plugin to demonstrate `addPipe`.
3.  **Building a UI Workflow Builder (Step-by-Step)**:
    *   **Setup**: Installing dependencies (`rete`, `rete-area-plugin`, `rete-connection-plugin`, `@retejs/lit-plugin`, etc.).
    *   **Initialization**: Setting up the Editor and Area.
    *   **Rendering**: Integrating `@retejs/lit-plugin` for UI (Custom Nodes/Controls).
    *   **Key Features for Builders**:
        *   **Context Menu**: Adding/deleting nodes (`rete-context-menu-plugin`).
        *   **History**: Undo/Redo support (`rete-history-plugin`).
        *   **Minimap**: Navigation aid (`rete-minimap-plugin`).
        *   **Auto Arrange**: Organizing the graph (`rete-auto-arrange-plugin`).
        *   **Modules**: Handling nested workflows (`rete-module-plugin`).
    *   **Engine Integration**: Processing the graph with `DataflowEngine`.
4.  **Complete Code Example**: A cohesive example combining all the above into a "WorkflowEditor" class.

## Verification Plan

### Manual Verification
- I will verify the generated code snippets against [llms-full.txt](file:///c:/Users/ODOO/Documents/workflow_automation_builder/llms-full.txt) to ensure API accuracy (e.g. imports, method names like `addPreset`, `use`).
- I will check that the Lit integration code matches the specific Lit examples in the source text (lines 1909-2127).
